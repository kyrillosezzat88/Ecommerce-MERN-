import { HeroSlider } from "@components/common";

const Home = () => {
  return (
    <section>
      <HeroSlider />
      <div className="container">tes</div>
    </section>
  );
};

export default Home;
